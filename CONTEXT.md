# Luoome 领域语言

## 核心概念

### 股票目录（StockUniverse）

回答“市场里有哪些可被系统识别的股票”。股票目录是按明确市场覆盖范围从外部数据源同步到
本地的低频基础资料集合，包含代码、交易所、名称、上市状态与来源观测信息。`stocks` 保存
系统使用的规范股票身份，目录同步记录负责说明该身份由哪个数据源、在哪次完整同步中观测到。

股票目录不等于全市场行情快照：目录描述相对稳定的股票身份，按日同步并持久化；行情快照描述
某一时刻的价格横截面，只做短 TTL 缓存。目录同步失败、空结果或分页不完整时保留上一版，
不会据此删除股票或判定退市。

### 规范日线（DailyBar）

系统内可供指标、战法和盯盘消费的日线统一使用前复权（`qfq`）价格坐标系。数据源原始
复权因子只作为可选溯源信息保存，不能把 raw OHLC 与因子并排返回后交给调用方换算，也不能
在因子缺失时用 `1` 伪装成功。盘后同步覆盖持仓、启用分组、研究档案和公司事件涉及的股票，
使昨收语义不依赖用户是否打开过行情页。

### 股票分组（StockGroup）

回答“看哪些股票”。分组维护成员集合，可来自手工列表、账户持仓、战法筛选或 LLM
筛选。动态来源通过成员快照与盘中盯盘解耦，因此同一个分组可以被多个场景复用。

### 盯盘方案（WatchPlan）

回答“这些股票发生什么时提醒”。一个方案引用一个股票分组，并配置一组触发规则、通知冷却
和启停状态。代码与持久化层暂时沿用 `StockPool` 名称以保持兼容；面向用户的 Web 与文档使用
“盯盘方案”。

Web 中不设独立“盯盘”信息孤岛：方案在其所属分组详情中配置；跨分组的运行心跳、手动试跑
和最近触发放在仪表盘。

公式分组使用的战法是成员生产条件，方案中的战法规则是盘中触发条件。两者职责不同，
允许引用不同的战法。

### 盯盘触发（WatchTrigger）

某只分组成员命中方案规则后产生的可审计事实，包含方向、理由、证据、行情快照和通知状态。

### 个性化报告（Report）

回答“某个周期发生了什么、哪些数据不可用、应去哪里继续研究”。报告按开盘、收盘和周度周期
持久化结构化事实、数据截止时间、来源状态与缺失原因，支持历史回看。同一报告周期重复运行
更新同一产物，不产生重复记录。报告可以自动生成事实摘要，但不会自动生成 Advice 或触发交易。

### A 股市场情绪快照（AShareSentimentSnapshot）

指定沪深 A 股交易日的市场情绪事实集合，包含指数、市场宽度、封板、炸板、连板、封单与热点等
独立维度。首期覆盖范围固定为 `CN_A_SHARES_SH_SZ`，不包含北交所；每个维度独立记录数据截止时间、
来源和可用状态。数据不可用不等于正常零值。

## 关键约束

- 股票目录的“全市场”必须带明确覆盖范围；首期指沪深 A 股，不暗示港股、美股或北交所。
- 股票目录同步只在完整快照通过校验后原子提交；缺失只标记 `missing`，不物理删除 `stocks`。
- 全市场行情快照中的价格不写入 `stocks`，也不批量写成分钟级 `PriceSnapshot`。
- `DailyBar` 只接受 `qfq`；存量 raw 日线不进入规范读取路径。
- 盘中涨跌规则缺少上一交易日收盘价时结果为 `unknown`，不得使用当日开盘价替代。
- 停用的分组不再向盯盘方案提供成员，即使数据库里仍有旧快照。
- 动态分组刷新失败或空结果时保留旧快照，并通过 `stale` 状态提示用户。
- 盯盘方案不拥有分组成员；删除被方案引用的分组会被拒绝。
- 个性化报告的结构化事实是事实源，Markdown 只是派生展示格式。
- 报告、策略研究和共振结果都不能绕过用户主动操作生成 Advice，也不会自动下单。
- 策略研究复用 Tactic、TacticSignal、StockGroup 和成员快照，不新增并行的 Strategy 执行实体。
- 建议与真实交易严格分离，盯盘触发不会自动下单。
